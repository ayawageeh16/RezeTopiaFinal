<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:title="@string/edit"
        android:id="@+id/edit_post_menu"
        android:icon="@drawable/ic_edit"/>

    <item android:title="@string/save_post"
        android:id="@+id/save_post_menu"
        android:icon="@drawable/ic_save"
        />

    <item android:title="@string/report"
        android:id="@+id/report_post_menu"
        android:icon="@drawable/ic_report"
        />
    <item android:title="@string/remove"
        android:id="@+id/remove_post_menu"
        android:icon="@drawable/ic_remove"
        />
</menu>